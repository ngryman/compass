var path = require('path');


module.exports = {

	TRAILING_SEPARATOR: new RegExp('\\' + path.sep + '$'),

	stripTrailingSeparator: function() {
		attributes.forEach(function(attr) {
			// TODO
		});
	}
};
